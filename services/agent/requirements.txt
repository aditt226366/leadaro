# Runtime deps for the agent workers (worker.py, dialer.py, post_call.py).
# Pinned to the versions in the working local venv so the container matches what
# runs on the dev box. Transitive deps (av, numpy, onnxruntime, aiohttp,
# websockets, livekit core/protocol, blingfire, local-inference) are pulled in
# by livekit-agents and the plugins below.
#
# The workers import db.py and llm.py from services/api (via a sibling-path
# insert), so anthropic/asyncpg/pydantic/python-dotenv are shared with the API.

# ── LiveKit runtime (worker.py: AgentSession + STT/TTS/VAD plugins) ──────────
livekit-agents==1.6.6
livekit-api==1.2.0
livekit-plugins-cartesia==1.6.6
livekit-plugins-deepgram==1.6.6
livekit-plugins-sarvam==1.6.6
livekit-plugins-silero==1.6.6
livekit-plugins-turn-detector==1.6.6

# ── shared with services/api (imported db.py / llm.py + brain.py) ────────────
anthropic==0.117.0
asyncpg==0.30.0
pydantic==2.10.5
python-dotenv==1.0.1
